import { Platform } from "react-native";
import icons from "./icons";
import images from "./images";
import { videos } from "./videos";

const ios = Platform.OS === "ios";

export { icons, images, videos, ios };
